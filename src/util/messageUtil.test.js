import messageUtil from './messageUtil';

describe('_combineSortedMessages()', () => {

    //Placeholder unit test
    it('returns something', () => {
        let mergedMessages = messageUtil._combineSortedMessages([], []);
        expect(mergedMessages).not.toBeNull();
    });

});
